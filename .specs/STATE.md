# STATE — Eterniza App

## Decisions

| ID | Decisão | Data | Origem |
|---|---|---|---|
| AD-001 | Desenvolvimento segue o `ROADMAP.md` etapa por etapa via spec-driven; **uma etapa = uma feature**; nunca adiantar trabalho de etapas futuras. | 2026-07-14 | Usuário |
| AD-002 | Os critérios "✅ Verificar" de cada etapa do roadmap **são os acceptance criteria** da feature. | 2026-07-14 | Usuário |
| AD-003 | Verificação **visual/de dispositivo é UAT interativo com o usuário** — nunca teste automatizado. Testes automatizados cobrem apenas o que é verificável em código (tokens, lógica, tipos). | 2026-07-14 | Usuário |
| AD-004 | Stack: React Native + Expo (TypeScript), `expo-router`, conforme `APP_BRIEF.md` §2. Design tokens conforme `DESIGN_SYSTEM.md` §9 — copiar exatamente, não reinterpretar. | 2026-07-14 | APP_BRIEF / DESIGN_SYSTEM |
| AD-005 | Cada etapa termina com o critério de verificação cumprido de verdade + **um commit atômico** (regra do ROADMAP.md). | 2026-07-14 | ROADMAP |
| AD-006 | Projeto fixado no **Expo SDK 54**: o dispositivo de teste é iPhone e o Expo Go da App Store está travado no SDK 54 (revisão da Apple pendente para 55+). Reavaliar upgrade na Etapa 6, quando o dev build (`expo-dev-client`) eliminar a dependência do Expo Go. | 2026-07-14 | Usuário (UAT Etapa 1) |
| AD-007 | **As telas usam como referência de composição/UX as imagens de `stitch_digital_disposable_social_camera/`** (screenshots do app Scene). Mapa: landing_6 = convite `/e/[slug]` (Etapa 5); landing_2/3 = câmera (Etapas 6–7); landing_4 = escolha de filtro (Etapa 7); landing_1/7/8 = evento/álbum com fotos trancadas + countdown + stats (Etapas 8–9); landing_5 = QR do host (Etapa 10); landing_9/10 = home/eventos do host (Etapa 10). **Hierarquia**: as imagens definem composição e conteúdo das telas; o `DESIGN_SYSTEM.md` prevalece na identidade visual (tipografia, cores, cantos retos — o Scene usa cantos arredondados; nós não). | 2026-07-14 | Usuário |
| AD-008 | `APP_BRIEF.md` atualizado (2026-07-15): `EventResponse` ganhou `coverImageUrl` (capa do convite, foto dos noivos) + `POST /api/events/{id}/cover` (host, multipart, substitui ao reenviar). Consumo pelo convidado incorporado como addendum à Etapa 5 (spec + código); o upload pelo host é escopo da Etapa 10. | 2026-07-15 | Usuário (atualização do brief) |
| AD-009 | **Efeito assinatura "Polaroid"** (spec em `.specs/design/polaroid-capture-reveal.md`): na captura, o telefone "imprime" uma Polaroid cuja química **para no borrado** (mantém o pilar da surpresa — nunca mostrar foto nítida antes do reveal); a química **completa até a nítida** na tela de Revelação. Entra nas Etapas 8 (captura) e 9 (revelação); depende de módulos nativos do rebuild da Etapa 7 (Skia obrigatório; haptics/audio/gesture a decidir na spec da 7). A piscada atual da câmera é o fallback. | 2026-07-15 | Usuário |
| AD-010 | **Preview ao vivo com matriz exata DESCARTADO** (UAT Etapa 7): o `useSkiaFrameProcessor` do VisionCamera v4 é experimental e **congela o app na new architecture** — ao anexar/desanexar, ao recriar por troca de filtro E sempre-anexado (3 variantes testadas no aparelho; upstream: vision-camera#3606 crash ao trocar filtro de color-matrix, #3517 leak de memória reproduzível no app de exemplo). Solução: **"véu de filme"** — preview nativo (estável, Etapa 6) + camada rgba com o tom do filtro (`previewTint` em `filters.ts`); o efeito EXATO fica nas miniaturas (Skia puro, sem câmera) e na foto final (pipeline offscreen). Revisitar preview exato quando o upstream estabilizar (v5/Nitro — reavaliar no pré-lançamento junto do upgrade de SDK). O Skia continua no build: pipeline da foto, miniaturas e Polaroid (AD-009) não dependem do frame processor. | 2026-07-16 | UAT (freezes no iPhone) |

## Handoff

- **Etapa 1 (`etapa-1-fundacao`):** ✅ CONCLUÍDA — Verifier PASS + UAT aprovado (2026-07-14). Commits f083aba..c89ae5e.
- **Etapa 2 (`etapa-2-componentes`):** ✅ CONCLUÍDA — Verifier PASS (38 testes, sensor 4/4) + UAT aprovado (2026-07-14). Commits 20a31e6..a05e932.
- **Etapa 3 (`etapa-3-navegacao`):** ✅ CONCLUÍDA — Verifier FAIL→fix→PASS (55 testes, sensor 5/5, lesson L-001) + UAT aprovado (2026-07-14). Commits 6c09f42..f9fc748. `eterniza://` literal re-verifica na Etapa 6.
- **Etapa 4 (`etapa-4-api-auth`):** ✅ CONCLUÍDA — Verifier ISSUES→fix→PASS (76 testes, sensor 5/5, lesson L-002) + UAT aprovado (2026-07-14): login real no iPhone com sessão persistida. Commits 3341a00..29f512f + fix.
- **Etapa 5 (`etapa-5-fluxo-convidado`):** ✅ CONCLUÍDA — Verifier PASS de primeira (91 testes, sensor 6/6) + UAT aprovado (2026-07-15). Commits cd92639..7723853. Evento de teste no backend: slug `casamento-ana-joao-1fuf`.
- **Etapa 6 (`etapa-6-camera-basica`):** ✅ CONCLUÍDA — Verifier inline PASS (sensor 6/6) + UAT aprovado (2026-07-15): 10 fotos reais no backend em 4032×3024 sem compressão; limite esgotou o obturador antes do 400; persistência do esgotado entre reaberturas; deep link `eterniza://` OK (fecha pendência da Etapa 3). Commits 6fd8262..17fb2f3 (inclui fixes de UAT: preview borrado → format com video max; latência de captura → photoQualityBalance balanced + estado capturing; efeito de captura → piscada do visor + clique mecânico). Decisões: VisionCamera **v4** (v5 = Nitro, cedo demais); **AD-006 mantida** (SDK 54; upgrade = pré-lançamento); repo GitHub privado `lucasbassetto/eterniza-app`.
- **Feature ativa:** `etapa-7-filtros` — código implementado e verificado (136 testes, sensor 6/6, verificação inline); commits b655a37..9ef343f. Rebuild no Mac FEITO (2026-07-16); **UAT em andamento** (fluidez ≥30fps, look dos filtros — calibrar curvas com o usuário —, foto filtrada no backend, regressão de fumaça). Fixes do UAT: (a) patch-package no `SkiaCameraCanvas` do VisionCamera — `<Canvas onLayout>` não é suportado na new arch (erro no console + medição quebrada), medição movida para um View wrapper; patch reaplicado via `postinstall` (mantido: inócuo e útil se o FP voltar); (b) tentativas com o frame processor Skia congelaram o app em todas as variantes → **AD-010**: preview = véu de filme (tint rgba) sobre o preview nativo da Etapa 6 (format de volta a video max/30fps); efeito exato nas miniaturas e na foto final. Calibrar no UAT: curvas das matrizes E tints do véu. (c) **Toque nos filtros morto no iOS**: bug upstream skia#3388 — na new arch, o Canvas do Skia **2.2.12** (pin do SDK 54) cai na interop layer (falta `codegenConfig.ios.componentProviders`, PR #3400) e o iOS descarta o toque que nasce na view dele; Pressable com Canvas dentro nunca dispara (obturador funcionava por não ter Canvas; jest passa porque fireEvent pula hit-testing). Workaround JS aplicado: `pointerEvents="none"` na miniatura decorativa + `decelerationRate="fast"`/`keyboardShouldPersistTaps` no ScrollView + pressed-opacity e tick háptico. ⚠️ **No próximo rebuild do Mac: subir @shopify/react-native-skia para ≥2.3.x** (fix real do #3388; adicionar `expo.install.exclude` para o expo doctor não reverter) e, até lá, NUNCA pôr Canvas Skia como filho direto de área tocável (vale para o Polaroid da Etapa 8). Notas: babel.config.js criado (plugin worklets-core fora do jest); babel-preset-expo devDep fixado em ~54; risco conhecido: orientação EXIF no pipeline (validar no UAT com foto retrato; correção é JS). Etapas 8–9 serão 100% JS após este build.
- **Addendum Etapa 5 (capa do convite, AD-008):** `/e/[slug]` exibe `coverImageUrl` como fundo full-bleed com scrim quando presente (fallback tipográfico quando `null`); `Input` ganhou prop `onDark`. Sem UI de upload ainda (Etapa 10) — testar via Postman no evento de teste se quiser validar visualmente antes de lá.
- **Build no Mac (roteiro):** clone + `npm install` + `cp .env.example .env` (IP real) + `npx expo run:ios --device`; Xcode → Signing → Personal Team na 1ª vez; iPhone com Modo Desenvolvedor ativo; confiar no certificado em Ajustes. CocoaPods se faltar: `brew install cocoapods`. Assinatura gratuita expira em 7 dias (reinstalar do Mac). Rebuild nativo só quando entrar módulo nativo novo; JS atualiza via Metro no Windows.
- **Ambiente de teste:** eventos `casamento-ana-joao-1fuf` (10/10 poses gastas — usar nas Etapas 8/9 para galeria trancada/reveal) e `festa-teste-2-545p` (poses livres). Backend local com MinIO (painel `localhost:9001`, minioadmin/minioadmin123, bucket eterniza-photos). ⚠️ Radar: URLs das fotos vêm como `http://localhost:9000/...` — o iPhone não carrega `localhost`; antes das Etapas 8–10 o backend precisa expor a URL pública com o IP da máquina (env var do MinIO/R2_PUBLIC_URL).
- **UAT do fluxo convidado (checklist):** link `exp://<url-do-tunnel>/--/e/{slug}` simula o QR; o `/--/` é obrigatório (sem ele o Expo Go abre a home). Gerar QR no terminal para escanear: `npx qrcode-terminal "<link>"`.
- **Ambiente de UAT com backend (checklist para as próximas etapas):** iPhone físico alcança a API em `http://<IP-da-máquina>:8080` desde que: (1) porta 8080 liberada no firewall do Windows (`New-NetFirewallRule ... -LocalPort 8080`); (2) `.env` com o IP de rede **atual** da máquina (DHCP muda — conferir com `ipconfig` e reiniciar o Metro com `--clear` após editar); (3) **permissão "Rede Local" do iOS ativada para o Expo Go** (Ajustes → Privacidade e Segurança → Rede Local) — sem ela o Safari alcança a API mas o fetch do app falha com "Network request failed".
- **Notas de ambiente:** teste em iPhone físico via `npx expo start --tunnel` (LAN bloqueada por firewall); Expo Go da App Store = SDK 54 (AD-006). Projeto em `src/app` (expo-router); tokens em `src/theme/theme.ts`; gate = `npx tsc --noEmit` + `npm run lint` + `npm test` (jest-expo). Conta de teste do host: `host@teste.com` / `eterniza123`. Adaptive icon do template mantido (Etapa 11).
