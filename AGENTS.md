# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

The project is pinned to Expo SDK 54 (see AD-006 in .specs/STATE.md): the test device is an iPhone and the App Store build of Expo Go only supports SDK 54. Do not upgrade the SDK without an explicit user decision.
